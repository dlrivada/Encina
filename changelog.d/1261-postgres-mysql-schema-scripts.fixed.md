- Rewrote the `000`-`004` schema scripts in `Encina.ADO.PostgreSQL`, `Encina.ADO.MySQL`, `Encina.Dapper.PostgreSQL` and `Encina.Dapper.MySQL` to use their own SQL dialect instead of SQL Server syntax (`[dbo].[...]`, `UNIQUEIDENTIFIER`, `NVARCHAR(MAX)`, `GO` batches) (#1261). The shipped scripts now create a schema that matches what each package's stores read and write, with correct column types and index syntax for PostgreSQL and MySQL.
