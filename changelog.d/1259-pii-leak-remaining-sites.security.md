- **Closed the remaining `EncinaError.Message` leaks found in the #1259 review** (#1274). The scheduler's batch-failure log and activity, the recoverability pipeline's transient/permanent error logs and delayed-retry-scheduling log, `DeadLetterManager.ReplayAllAsync`, `DeadLetterCleanupProcessor`'s wrapped exception, the outbox/inbox/scheduling health checks, `ContentRouter`, `InboxOrchestrator`'s cached response envelope, `RoutingSlipRunner`, the low-ceremony `SagaRunner` (including the state persisted by `StartCompensationAsync`) and `ScatterGatherRunner` now carry only the error code in what they log, tag, store or return.
