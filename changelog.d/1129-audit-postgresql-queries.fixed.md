- Fixed `IAuditStore.GetByEntityAsync` on the PostgreSQL ADO.NET provider, and `IAuditStore.GetByUserAsync` on both the PostgreSQL ADO.NET and Dapper providers, returning `Left` (error) when the optional `entityId`, `fromUtc` or `toUtc` filter was `null` (`42P08: could not determine data type of parameter`), by adding explicit `::text` / `::timestamp` casts to the reused nullable parameters in the generated SQL.
