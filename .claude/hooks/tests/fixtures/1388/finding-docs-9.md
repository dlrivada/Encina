`docs/messaging/index.md:14-24` ("Messaging Transports" table, "Encina supports **10 messaging transports**"): a count with no citing sentence (date + command) as SKILL.md §3 house rule 2 requires for "counts that no dashboard measures (number of packages, providers, tests)". The number happens to match AGENTS.md §5's own count of 10 existing transports, so it is not wrong today, but the page does not follow the citation convention. Lower severity than the coverage/mutation cases the rule is mainly aimed at, since AGENTS.md §5 is itself an authoritative, versioned source for this fact, not a hand-typed guess.
