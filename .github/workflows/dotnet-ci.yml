name: .NET Quality Gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  DOTNET_NOLOGO: true
  DOTNET_SDK_VERSION: "10.0.x"

jobs:
  # Quick quality checks - format and build
  quality-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_SDK_VERSION }}

      - name: Cache NuGet packages
        uses: actions/cache@v5
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/Directory.*.props') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore
        run: dotnet restore Encina.slnx

      - name: Enforce formatting
        run: dotnet format Encina.slnx --verify-no-changes

      - name: Build with analyzers
        run: dotnet build Encina.slnx --configuration Release --no-restore -warnaserror

  # Critical tests only - Core, Messaging, and main packages
  critical-tests:
    needs: quality-check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: ${{ env.DOTNET_SDK_VERSION }}

      - name: Cache NuGet packages
        uses: actions/cache@v5
        with:
          path: ~/.nuget/packages
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/Directory.*.props') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Restore
        run: dotnet restore Encina.slnx

      - name: Build
        run: dotnet build Encina.slnx --configuration Release --no-restore

      - name: Test Unit Tests (Consolidated)
        run: |
          dotnet test tests/Encina.UnitTests/Encina.UnitTests.csproj -c Release --no-build \
            --collect "XPlat Code Coverage" \
            --results-directory artifacts/test-results || true

      - name: Test Integration Tests (Consolidated)
        run: |
          dotnet test tests/Encina.IntegrationTests/Encina.IntegrationTests.csproj -c Release --no-build \
            --collect "XPlat Code Coverage" \
            --results-directory artifacts/test-results || true

      - name: Test Contract Tests (Consolidated)
        run: |
          dotnet test tests/Encina.ContractTests/Encina.ContractTests.csproj -c Release --no-build \
            --collect "XPlat Code Coverage" \
            --results-directory artifacts/test-results || true

      - name: Test Property Tests (Consolidated)
        run: |
          dotnet test tests/Encina.PropertyTests/Encina.PropertyTests.csproj -c Release --no-build \
            --collect "XPlat Code Coverage" \
            --results-directory artifacts/test-results || true

      - name: Test Guard Tests (Consolidated)
        run: |
          dotnet test tests/Encina.GuardTests/Encina.GuardTests.csproj -c Release --no-build \
            --collect "XPlat Code Coverage" \
            --results-directory artifacts/test-results || true

      - name: Restore .NET tools
        run: dotnet tool restore

      - name: Generate coverage report
        run: |
          dotnet tool run reportgenerator \
            -reports:"artifacts/test-results/**/coverage.cobertura.xml" \
            -targetdir:"artifacts/coverage" \
            -reporttypes:"Html;HtmlSummary;TextSummary;JsonSummary" || true

      - name: Publish coverage summary
        if: always()
        run: |
          if [ -f "artifacts/coverage/Summary.txt" ]; then
            {
              echo '## Coverage Summary'
              echo '```'
              cat artifacts/coverage/Summary.txt
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-report
          path: artifacts/coverage
          if-no-files-found: ignore
